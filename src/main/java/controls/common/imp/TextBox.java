package controls.common.imp;

import controls.base.imp.Editable;
import controls.common.ITextBox;

public class TextBox extends Editable implements ITextBox {
    public TextBox(String locator) {
        super(locator);
    }
}
