package controls.common.imp;

import controls.base.imp.Clickable;
import controls.common.ILabel;

public class Label extends Clickable implements ILabel {
    public Label(String locator) {
        super(locator);
    }
}
